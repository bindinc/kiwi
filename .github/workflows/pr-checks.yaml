name: pr-checks

on:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_DB: kiwi_test
          POSTGRES_USER: kiwi
          POSTGRES_PASSWORD: kiwi
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U kiwi -d kiwi_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          tools: composer:v2

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Install PHP dependencies
        run: composer install --prefer-dist --no-interaction --no-progress

      - name: Run PHPUnit
        run: php bin/phpunit

      - name: Run frontend tests
        run: node --test $(find tests/frontend -name '*.test.mjs' -print | sort)

      - name: Run guardrails
        run: node script/check

      - name: Verify startup migration ordering and failure handling
        run: python3 -m unittest discover -s tests/scripts -p 'test_app_entrypoint.py'

      - name: Build production image
        run: docker build -f infra/docker/app/Dockerfile --target prod --build-arg APP_VERSION=v0.0.0-ci -t kiwi:ci .

      - name: Verify embedded release version
        run: |
          docker run --rm --entrypoint php kiwi:ci -r 'exit(getenv("APP_VERSION") === "v0.0.0-ci" ? 0 : 1);'
